// operador de strin (string operator)

// comparison (comparação)
console.log('a' == 'b')

// concatenation (concatenação)
let alpha = 'alpha'
alpha += 'bet'
console.log(alpha + 345)

// retorna a uniao de duas "strings"